import React from "react";


const FoodItem = (prop) => {
    const  {image, heading, price, text, rate  } = props.cuisine
  return (
    <div className>

    </div>
  );
};

export default FoodItem;
